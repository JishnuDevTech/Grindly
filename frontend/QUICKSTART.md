# Quickstart

1. Use Node.js 18 or newer.
2. Run `npm install` from this directory.
3. Run `npm run dev`.
4. Open the URL printed by Vite.

To reset the onboarding screen, clear the `grindly_onboarding_complete` item from browser local storage.
