import { useMemo, useState } from 'react';
import { motion } from 'framer-motion';
import { Check, Coins, Gift, Lock, Sparkles, Crown, Frame, BadgeCheck, Orbit, Palette, Shield } from 'lucide-react';
import { getRarityColor } from '../data/mockData';

const categories = ['All', 'Profile Frame', 'Nameplate', 'Theme', 'XP Effect', 'Avatar Effect', 'Badge'];

export default function Shop({ user, items, onPurchase }) {
  const [category, setCategory] = useState('All');
  const filteredItems = useMemo(() => category === 'All' ? items : items.filter((item) => item.category === category), [category, items]);
  return <div className="min-h-screen bg-ink"><div className="mx-auto max-w-6xl px-5 py-7 sm:px-8 lg:px-12 lg:py-10"><header className="flex flex-col justify-between gap-5 border-b border-white/8 pb-7 sm:flex-row sm:items-end"><div><p className="eyebrow">The Vault</p><h1 className="mt-2 font-display text-4xl font-extrabold">Make it yours.</h1><p className="mt-2 text-sm text-muted">Cosmetics earned through real progress. No shortcuts, no power-ups.</p></div><div className="flex items-center gap-3 rounded-2xl border border-[#ffd27a]/20 bg-[#ffd27a]/8 px-4 py-3"><Coins size={18} className="text-[#ffd27a]" /><div><p className="eyebrow">Your balance</p><p className="font-display text-lg font-extrabold text-[#ffd27a]">{user.coins.toLocaleString()}</p></div></div></header><div className="mt-7 flex gap-2 overflow-x-auto pb-1">{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full border px-3 py-1.5 text-xs font-semibold transition ${category === item ? 'border-lime/40 bg-lime/10 text-lime-soft' : 'border-white/10 bg-white/[.03] text-muted hover:bg-white/[.07]'}`}>{item}</button>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filteredItems.map((item, index) => <ShopItem key={item.id} item={item} index={index} onPurchase={onPurchase} canAfford={user.coins >= item.price} />)}</div><div className="mt-8 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-lime/20 bg-gradient-to-br from-[#1d2d20] to-panel p-5"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-lime text-ink"><Gift size={19} /></div><div><p className="font-display font-bold">Mystery chest</p><p className="text-xs text-muted">A surprise for consistent grinders</p></div></div><p className="mt-4 text-sm leading-6 text-slate-300">Some of the rarest cosmetics can’t be bought. Keep your streak alive and watch for milestone drops.</p></div><div className="rounded-2xl border border-white/10 bg-panel p-5"><div className="flex items-center gap-2"><Sparkles size={16} className="text-lime" /><p className="eyebrow">Economy rule</p></div><p className="mt-4 font-display text-lg font-bold">Earned looks better.</p><p className="mt-1 text-sm leading-6 text-muted">Cosmetics express progress, but never give gameplay power. Your work stays the reward.</p></div></div></div></div>;
}

function ShopItem({ item, index, onPurchase, canAfford }) { const Icon = { 'Profile Frame': Frame, Nameplate: BadgeCheck, Theme: Palette, 'XP Effect': Orbit, 'Avatar Effect': Sparkles, Badge: Shield, 'Trophy Display': Crown }[item.category] || Sparkles; const rarity = item.rarity || 'Common'; return <motion.article initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .05 }} className={`group relative overflow-hidden rounded-2xl border bg-panel p-4 ${item.locked ? 'border-white/8' : 'border-white/10 hover:border-lime/25'}`}><div className={`relative flex h-36 items-center justify-center overflow-hidden rounded-xl bg-gradient-to-br ${rarityBackgrounds[rarity] || rarityBackgrounds.Common} transition duration-300 group-hover:scale-[1.02]`}><div className="absolute inset-0 opacity-40 [background-image:radial-gradient(circle_at_30%_20%,white_0,transparent_35%),linear-gradient(120deg,transparent_35%,rgba(255,255,255,.16),transparent_65%)]" /><div className="relative grid h-20 w-20 place-items-center rounded-[1.6rem] border border-white/30 bg-black/20 text-white shadow-2xl backdrop-blur-sm"><Icon size={34} strokeWidth={1.5} /></div><span className="absolute bottom-3 left-3 rounded-full border border-white/20 bg-black/20 px-2 py-1 font-mono text-[.58rem] font-bold tracking-widest text-white/80">{rarity.toUpperCase()}</span></div><div className="mt-4 flex items-start justify-between gap-3"><div><p className="font-display font-bold">{item.name}</p><p className="mt-1 text-xs text-muted">{item.category} cosmetic</p></div><span className={`font-mono text-[.6rem] font-bold ${getRarityColor(rarity)}`}>{rarity.toUpperCase()}</span></div>{item.locked ? <div className="mt-5 flex items-center gap-2 rounded-xl bg-white/5 px-3 py-2 text-xs text-muted"><Lock size={13} /> {item.unlocksAt || 'Unlock in Chest'}</div> : item.owned ? <div className="mt-5 flex items-center justify-center gap-2 rounded-xl bg-[#5bdc96]/10 py-2.5 text-xs font-bold text-[#8ff0b6]"><Check size={14} /> Owned</div> : <button onClick={() => onPurchase(item.id)} disabled={!canAfford} className={`mt-5 flex w-full items-center justify-center gap-2 rounded-xl py-2.5 text-xs font-bold transition ${canAfford ? 'bg-white/8 text-white hover:bg-lime hover:text-ink' : 'cursor-not-allowed bg-white/5 text-muted'}`}><Coins size={14} className="text-[#ffd27a]" /> {item.price.toLocaleString()}</button>}</motion.article>; }

const rarityBackgrounds = { Common: 'from-slate-700/60 to-slate-900', Rare: 'from-sky-500/30 to-slate-900', Epic: 'from-violet-500/35 to-slate-900', Elite: 'from-rose-500/35 to-slate-900', Legendary: 'from-amber-400/40 to-slate-900', Mythic: 'from-fuchsia-500/45 via-indigo-900 to-slate-950' };
